#include <stdio.h>
#include <string.h>

int main(void)
{
	int i = 0;
	int arr[10];
	
	memset(arr, , sizeof(arr));
	
	for(; i < sizeof(arr) / sizeof(int); i++)
	{
		printf("arr[%d]ÀÇ °ª: %d\n", i, arr[i]);
	}
	return 0;
	
}
